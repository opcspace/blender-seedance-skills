[project]
name = "blender-precision-mcp"
version = "0.1.0"
description = "Allow-listed precision modeling MCP companion for Blender"
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.3,<2",
  "httpx>=0.24",
]

[tool.uv]
package = false
